what is Git and what is Github?
Git is a distributed version control system. we need to download and install.

git website -->download --> windows ---> 64 setup ---> next --> check code editor --> next until install and finish

check version by cmd ---> "git -v"

Goto Github and create an account

goto terminal
git init
git add .
git commit -m "message"
for first time, git config -g user.email "emailid"
git config -g user.name "username"

create a respository in github and talk its link

git remote add origin "link"
git push -u origin main/master

for first time it ask you to login and authorize